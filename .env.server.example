# =========================================================================
# MyKeyManager - Configurazione Server Esterno
# Copia questo file come .env e personalizza i valori
# =========================================================================

# PREFISSO CONTAINER (per evitare conflitti)
CONTAINER_PREFIX=mykeymanager

# VERSIONI IMMAGINI
BACKEND_VERSION=v1.1.2
FRONTEND_VERSION=v1.1.1

# ====================== PORTE DEL SERVER ======================
# Modifica queste porte secondo le tue esigenze e firewall

# Frontend Web Interface
FRONTEND_PORT=8080

# Backend API
BACKEND_PORT=8001

# Database PostgreSQL (usa una porta non standard per sicurezza)
DB_PORT=5432

# Redis Cache
REDIS_PORT=6379

# ====================== RETE ======================
NETWORK_NAME=mykeymanager-network

# ====================== DATABASE ======================
POSTGRES_DB=mykeymanager
POSTGRES_USER=mykeymanager
# CAMBIA QUESTA PASSWORD IN PRODUZIONE!
POSTGRES_PASSWORD=SecureServerPass123

# ====================== SICUREZZA ======================
# CAMBIA QUESTA CHIAVE SEGRETA IN PRODUZIONE!
SECRET_KEY=your-production-secret-key-here

# ====================== URL PUBBLICI ======================
# Sostituisci con il tuo dominio/IP pubblico del server
# Esempi:
# - http://your-server.com:8080
# - https://mykeymanager.yourdomain.com
# - http://123.456.789.101:8080

# URL del frontend (per CORS)
ALLOWED_ORIGINS=http://localhost:8080

# CORS Origins (JSON array)
CORS_ORIGINS=["http://localhost:8080","http://your-server-ip:8080"]

# URL dell'API per il frontend
API_URL=http://localhost:8001/api

# ====================== EMAIL SMTP (OPZIONALE) ======================
# Configurazione per invio email (notifiche, reset password, etc.)
SMTP_HOST=smtp.your-domain.com
SMTP_PORT=587
SMTP_USER=noreply@your-domain.com
SMTP_PASSWORD=your-smtp-password
SMTP_TLS=true

# ====================== ESEMPIO CONFIGURAZIONI SERVER ======================

# --- Server con IP pubblico ---
# ALLOWED_ORIGINS=http://123.456.789.101:8080
# CORS_ORIGINS=["http://123.456.789.101:8080"]
# API_URL=http://123.456.789.101:8001/api

# --- Server con dominio ---
# ALLOWED_ORIGINS=https://mykeymanager.yourdomain.com
# CORS_ORIGINS=["https://mykeymanager.yourdomain.com"]
# API_URL=https://api.yourdomain.com/api

# --- Server con reverse proxy (Nginx/Traefik) ---
# FRONTEND_PORT=3000
# BACKEND_PORT=3001
# ALLOWED_ORIGINS=https://mykeymanager.yourdomain.com
# API_URL=https://mykeymanager.yourdomain.com/api
